//This class should be the context provider for entire app
// Including: scale notes, 

import { createContext } from 'react';

const FretboardTrainerContext = createContext(undefined);

export default FretboardTrainerContext;
